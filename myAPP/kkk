for file in $(curl -s http://content.example.com/courses/  |
                  grep href |
                  sed 's/.*href="//' |
                  sed 's/".*//' |
                  grep '^[a-zA-Z].*'); do
    curl -s -O http://content.example.com/courses/$file
done
